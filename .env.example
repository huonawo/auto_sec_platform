# AutoSec Platform Environment Variables
# Copy this file to .env and fill in the values before running docker-compose up.

# Redis password — required by backend, worker, and orchestrator.
# Must match across all services. Generate a strong password for production.
REDIS_PASSWORD=change_me_to_a_strong_password

# ── LLM API Configuration ─────────────────────────────────────────────────────
# Required for AI-powered vulnerability analysis (POST /ai/analyze).
# Without AI_API_KEY, the system falls back to rule-based analysis only.

# API key for the LLM service
AI_API_KEY=

# OpenAI-compatible API endpoint (default: https://api.xiaomimimo.com/v1)
# AI_API_BASE_URL=https://api.xiaomimimo.com/v1

# Model name (default: mimo-v2.5-pro)
# AI_MODEL=mimo-v2.5-pro
